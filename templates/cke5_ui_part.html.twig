<cke5-ui-part
    id="{{ id }}"
    {% if class is not null %}class="{{ class }}"{% endif %}
    {% if style is not null %}style="{{ style }}"{% endif %}
    {% if editorId is not null %}data-cke-editor-id="{{ editorId }}"{% endif %}
    data-cke-name="{{ name }}"
    data-turbo-temporary
></cke5-ui-part>
